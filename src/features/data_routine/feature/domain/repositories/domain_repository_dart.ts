// G:\Projects\Flutter\dataroutine3\lib\features\tasks\domain\repositories\category_repository.dart

// import path from "path";
// import { cap, pluralConvert } from "../../../../../utils/text_work/text_util";

// export const domainRepoPath = (fPath: string, driftClass: string) => path.join(fPath, "domain", "repositories", `${driftClass}_repository.dart`);

// export const domainRepoCont = (driftClass: string) => {
//   const d = driftClass;
//   const Ds = cap(pluralConvert(driftClass));
//   const D = cap(d);

//   return `
// import '../entities/${d}.dart';

// abstract class ${D}Repository {
//   Future<List<${D}Entity>> get${Ds}();
//   Future<${D}Entity> get${D}ById(int id);
//   Future<int> create${D}(${D}Entity ${d});
//   Future<void> update${D}(${D}Entity ${d});
//   Future<void> delete${D}(int id);
// }
// `;
// };


